// This file is intentionally empty — Partner model was defined here but
// fetchPartner() and updatePartner() (the only callers) have been removed
// as dead code (non-existent route, never called from UI).
