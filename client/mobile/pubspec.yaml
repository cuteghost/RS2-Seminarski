name: ebooking
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

# Dependencies specify other packages that your package needs in order to work.
#
# Upgrade policy for this project:
#  - Do NOT run `flutter pub upgrade --major-versions` as a blanket command.
#    Bump packages individually and read the changelog for major versions.
#  - Only adopt a version that has been on pub.dev for at least 7 days
#    (supply-chain soak period). Use `flutter pub outdated` to see candidates.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  # Exact pin: 3.2.1 was published 2026-08-09 and is younger than the 7-day
  # supply-chain soak period. Relax to ^3.2.1 on/after 2026-08-16.
  table_calendar: 3.2.0
  google_maps_flutter: ^2.18.0
  geolocator: ^14.0.3
  permission_handler: ^13.0.0
  http: ^1.6.0
  # 11.0.0 was published 2026-08-06; held back by the 7-day supply-chain rule.
  flutter_secure_storage: ^10.3.1
  provider: ^6.1.5+1
  flutter_facebook_auth: ^7.2.0
  image_picker: ^1.2.3
  google_sign_in: ^7.2.0
  wechat_assets_picker: ^10.1.3
  # Unmaintained since 1.1.2 (2024-02-09) and pins web_socket_channel to 2.x.
  # No newer release exists; migrate to signalr_netcore when this becomes a blocker.
  signalr_core: ^1.1.2
  webview_flutter: ^4.14.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# signalr_core 1.1.2 pins web_socket_channel ^2.4.3, which drags in package:web ^0.5.0
# and blocks google_maps_flutter >=2.18.0 (needs package:web ^1.0.0).
# signalr_core only uses WebSocketChannel, WebSocketChannel.connect and IOWebSocketChannel
# (lib/src/transports/web_socket_channel_{api,html,io}.dart) — all unchanged in
# web_socket_channel 3.x — so this override is source-compatible.
# Remove it once signalr_core is replaced by signalr_netcore.
dependency_overrides:
  web_socket_channel: ^3.0.3

  # Transitive packages whose newest release is younger than the 7-day
  # supply-chain soak period. Each pin below is still inside the range its
  # parent package declares, so nothing is being force-downgraded past a
  # real constraint. Drop a pin once its newer version has aged past 7 days.
  photo_manager: 3.11.0 # 3.12.0 published 2026-08-09
  permission_handler_apple: 9.5.0 # 9.6.0 published 2026-08-10
  flutter_secure_storage_linux: 3.0.1 # 3.0.2 published 2026-08-06
  flutter_secure_storage_platform_interface: 2.0.2 # 2.0.3 published 2026-08-05
  win32: 6.3.0 # 6.4.0 published 2026-08-05

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/eBooking.png"
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets: 
    - assets/images/
    - assets/Icons/azure-blue-dot.png
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
